let numingresado: number;

for (let contador: number = 1; contador <= 20; contador++) {
  //numingresado = prompt("ingrese un numero positivo");
  if (numingresado % 2 == 0 || numingresado % 3 == 0) {
    console.log(numingresado);
  }
}
